// E2E tests for onboarding flow – tests first job type creation.
import { test, expect } from '@playwright/test'

test('should require job type creation on first login', async ({ page }) => {
  // Placeholder – will be implemented with actual onboarding flow
  expect(true).toBe(true)
})
